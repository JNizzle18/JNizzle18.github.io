(function($) {
  $.fn.AlertBox = function(options) {

    // default settings:
    var defaults = {
      sumbitSelector = "#submit",
      cancelSelector = "#cancel",
      callback = null
    };

    var settings = $.extend({}, defaults, options);

    // return this.each(function() {
    // Plugin code would go here...
    // });

  };
}(jQuery));
